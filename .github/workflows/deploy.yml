name: Django React Auto Deployment Manager

on:
  push:
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-22.04
    steps:
      - name: Run Checks
        uses: OZ-Coding-School/oz_01_collabo-004/.github/workflows/checks.yml@main

  build:
    runs-on: ubuntu-22.04
    needs: ci
    steps:
      - name: EC2 ssh connection and deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}			# 연결할 EC2 
          username: ${{ secrets.SSH_USERNAME }}	# 사용자
          password: ${{ secrets.SSH_PASSWORD }}				# pem 파일 내용
          command_timeout: 5m						# timeout 시간 설정
          script: |
              cd oz_01_collabo-004/frontend
              rm 
              git pull origin main
              cd frontend
              npm run build
              sudo systemctl restart uwsgi
              sudo systemctl restart nginx
